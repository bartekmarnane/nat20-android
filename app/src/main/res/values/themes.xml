<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme. Compose drives the real theming; this is just the
         window/launch theme the manifest points at. Day/night handled by the
         platform DayNight parent. Swap for the ink-on-parchment theme in step A7e. -->
    <style name="Theme.Nat20" parent="android:Theme.DeviceDefault.DayNight.NoActionBar" />
</resources>
