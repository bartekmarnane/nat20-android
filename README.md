# Nat20 (Android)

The Android port of Nat20 — an app for managing D&D characters with pluggable rulesets.

Each character has a change log so you can see what shifted over time — level-ups, equipment, etc. The character sheet, spell list, and available options reflect the ruleset you've picked. The shipping iOS app (`../nat20-ios`) is the reference implementation; this README tracks bringing the same experience to Android.

## Stack

| | |
|---|---|
| **Platform** | Android (phone + tablet), `minSdk = 26`, `target/compileSdk = 35` |
| **Language / UI** | Kotlin 2.0 + Jetpack Compose (Material3) |
| **Persistence** | Room (SQLite), local-only to start — behind a repository interface so sync can slot in later |
| **Serialization** | `kotlinx.serialization` for the payload/event JSON blobs |
| **State** | ViewModel + `StateFlow` (iOS's `@Observable` store has no direct equivalent) |
| **Rulesets** | D&D 5e (2014) first; D&D 5e (2024) and Pathfinder 2e (Remaster) planned — see Build order |
| **On-device AI** | Gemini Nano (AICore / ML Kit GenAI) where supported, availability-gated like iOS gates FoundationModels |
| **Billing** | Google Play Billing (one free character + one-time IAP unlock) |
| **Application ID** | `au.com.evonet.nat20` (mirrors the iOS bundle ID) |

## Relationship to the iOS app

This is a **parallel native Kotlin port**, not a shared binary. iOS keeps its Swift packages; Android reimplements the same domain model and rules engines idiomatically in Kotlin. Two codebases, each native to its platform. (Kotlin Multiplatform for the shared domain was considered and deferred — see Open decisions.)

> **iOS has moved well past where this plan started.** When this backlog was first written the iOS app was **22 steps and a single ruleset**. It is now **26 steps across three rulesets** (D&D 5e 2014, D&D 5e 2024, Pathfinder 2e Remaster) plus onboarding and editable journals. The build order below has been reconciled to that reality. The reassuring part: iOS confirms that adding the two new rulesets needed **zero changes** to `Ruleset` / `Character` / persistence — `rulesetID` isolation held — so the Android **`:domain` abstraction (A1–A2) is already validated**; the entire multi-ruleset story lives *above* the domain, in the ruleset modules.

**What we reuse directly from `../nat20-ios`:**

- **SRD / open-licensed catalogues** — the platform-agnostic JSON under each iOS ruleset package's `Resources/`. Copy verbatim into Android resources. Coverage now spans:
  - **5e 2014** — SRD 5.1 (CC BY 4.0, `5e-bits/5e-database`): 319 SRD spells (+ Tasha's/Xanathar's supplement spells), 17 races, 12 classes (all subclasses L1–L20), 15 backgrounds, feats/fighting-styles/metamagic/invocations/pact-boons, armor/weapons/gear, **334 SRD monsters**.
  - **5e 2024** — SRD 5.2 (CC BY 4.0): 339 spells with rules text, weapons/armor/gear, **330 monsters** (Open5e `srd-2024`).
  - **PF2e Remaster** — **ORC License** (Archives of Nethys), the *complete* ruleset is legally sourceable: spells, items/runes, and the **445-creature Monster Core**.
  - **Licensing:** carry attribution verbatim (Credits screen, A9). SRD 5.1/5.2 + 5e-bits + Open5e are CC BY 4.0; PF2e Monster Core / AoN is ORC; the five typefaces are SIL OFL 1.1. **Tasha's/Xanathar's spell content is *not* openly licensed** — mirror the iOS stance (no broad reuse).
- **Domain shape** — `Ruleset` / `Character` / `CharacterPayload` / `CharacterEvent` / `CharacterIntent` and the intent-logged-mutation pattern (`Campaign.apply`). The iOS `Packages/Domain` + the per-ruleset packages are the spec.
- **The full roadmap + rules-coverage notes** — [`../nat20-ios/README.md`](../nat20-ios/README.md) is the canonical feature definition (26 steps + a long "5e rules — coverage gaps" list). When in doubt about how a mechanic should behave, that's the source of truth.
- **Visual design language** — the ink-on-parchment look (Cinzel / Cormorant Garamond / EB Garamond / IM Fell English typefaces under SIL OFL 1.1; parchment ground; gold accents; codex-shell tab layout; the d20 app icon). Match it on Android with Compose theming.

**Risk to manage:** parallel codebases can drift. When a rule is fixed or refined on one platform, mirror it on the other.

## Module layout (multi-ruleset, anticipated up front)

iOS refactored at its step 23 into a **shared-core + thin-version-package** shape: edition-agnostic 5e machinery in `DnD5eCore`, shared parchment chrome in `DnD5eUICore`, then `DnD5e` (2014) / `DnD5e2024` as thin packages on top; PF2e gets its own `PathfinderCore` + `Pathfinder`. **We mirror that seam on Android from the start** rather than extracting it later, so the 2014 engine grows in the right module and the second/third rulesets slot in without a reshuffle.

```
:domain                 pure Kotlin/JVM, NO Android deps  — Ruleset/Character/Payload/Intent/Event + Campaign.apply        [A1–A2 ✅]
:ruleset-dnd5e-core     pure Kotlin/JVM, deps :domain     — edition-agnostic 5e machinery: Ability + modifier math,
                                                            proficiency-by-level, slot tables, effects model              (mirrors DnD5eCore)
:ruleset-dnd5e-2014     pure Kotlin/JVM, deps core        — 2014 payload, catalogues, intents, level-up                  (mirrors DnD5e)
:ruleset-dnd5e-2024     pure Kotlin/JVM, deps core        — 2024 edition                          (mirrors DnD5e2024)    [created at A18]
:ruleset-pf2e-core      pure Kotlin/JVM, deps :domain     — PF2e maths: proficiency ladder, degrees of success           (mirrors PathfinderCore) [A19]
:ruleset-pf2e           pure Kotlin/JVM, deps pf2e-core   — PF2e payload, catalogues, flows       (mirrors Pathfinder)   [A19]
:data                   Android library                   — Room entities + DAOs + CharacterRepository (the sync seam)
:app                    Android app (Compose)             — UI, ViewModels, navigation, entry point
```

- **`:domain` and every `:ruleset-*` module stay free of Android imports** so they're JVM-unit-testable (mirrors iOS, where the packages run `swift test` on the host). UI lives **only** in `:app`.
- **Shared parchment chrome** (iOS's `DnD5eUICore` — theme, wizard atoms, the `ReferenceCodex*` browsers): on Android, UI rules say this lives in `:app`, organised under a `theme` / `codex` package. Extract a `:ui-core` Android library only if a second ruleset's UI actually justifies it (don't pre-extract empty).
- **Only `:ruleset-dnd5e-core` + `:ruleset-dnd5e-2014` are created now.** The 2024 / PF2e modules are listed for orientation but get scaffolded when their build steps land — no empty modules carrying build cost ahead of content.

## iOS → Android translation map

| iOS | Android | Notes |
|---|---|---|
| SwiftData `@Model` (thin JSON envelopes, `payloadData: Data`) | Room entity + `kotlinx.serialization` blob | persistence is already envelope-style — clean map |
| Codecs `CharacterCodec` / `CampaignCodec` | domain ↔ Room row translators | keep the seam |
| `@Observable` store (`CharacterStore`) | ViewModel + `StateFlow` | `store.apply(intent)` → `Campaign.apply` → persist + journal |
| Intent structs | sealed-class hierarchy of intents | pure logic, voluminous, correctness-critical |
| `NavigationStack` + closure routing | Navigation Compose | sheet/picker enums → routes or bottom-sheet state |
| 6-tab paged `TabView` (CodexShell) | `HorizontalPager` + tab row | |
| StoreKit 2 (`PatronStore`) | Play Billing | A14 |
| Apple FoundationModels (`@Generable`) | Gemini Nano + JSON-schema prompting | gated; degrade gracefully |
| `ImageRenderer`→PDF export | `PdfDocument`/Canvas hand-built layout | no Compose equivalent to SwiftUI-view PDF |
| Shared-core + version packages (`DnD5eCore`/`DnD5e`/`DnD5e2024`) | `:ruleset-dnd5e-core` + `:ruleset-dnd5e-2014` + `:ruleset-dnd5e-2024` | same seam, Gradle modules |

## Greenfield simplification vs iOS

iOS carries ~1200 lines of **tolerant `Codable`** with legacy-field migration branches (years of shipped schema changes for existing users). **Android is greenfield with no users**, so port only the *current* payload schema — skip the legacy-migration branches. This removes the single biggest porting risk. (Keep the schema versioned in Room so future migrations are clean.)

## Build order

The iOS app was built abstraction-first and shipped in a working order that proved out well; Android follows the same spine. A-step numbers map to iOS *features*, not 1:1 to iOS step numbers (the cross-references note which iOS step each mirrors).

### Foundation

- [x] **A1. Project skeleton** — Android Studio project, Gradle (Kotlin DSL), git repo + `.gitignore`, multi-module structure. SRD catalogues + fonts copied in. *(Builds with Android Studio's bundled JBR — no standalone JDK on the dev machine; set `JAVA_HOME` to it.)*
- [x] **A2. Core domain abstractions** — `Ruleset`, `Character`, `CharacterPayload`, `CharacterEvent`, `CharacterIntent` as Kotlin interfaces + value types, plus `LoggedEvent`, `NoteKind`, `Summon`/`Creature`. Intents validate-then-return via `CharacterIntent.applyTo → IntentResult`. Codec surface uses JSON `String` (kotlinx.serialization). JUnit5 tests. *(iOS 1–2.)*
- [x] **A3. D&D 5e ruleset, minimal slice** — `DnD5eRuleset` + `DnD5ePayload` (race, classes/`ClassEntry`, derived level, six abilities via `AbilityScores`, HP). `Ability` enum + modifier math; proficiency-bonus-by-level. First intents (`TakeDamage`/`Heal`/`GainTempHp`/`LevelUp`) + events + JSON codec. `LevelUpMath` (HP). JUnit5 tests. *(iOS 3. Currently all in `:ruleset-dnd5e` — split in A3.5.)*
- [x] **A3.5. Module split for multi-ruleset** — renamed `:ruleset-dnd5e` → `:ruleset-dnd5e-2014`; extracted the edition-agnostic A3 machinery into a new `:ruleset-dnd5e-core` (`Ability`/`AbilityScores` + modifier math, `HpChoice`/`LevelUpMath`/`DnD5eClasses`, `Proficiency.bonus`), package `…dnd5e.core`. `:ruleset-dnd5e-2014` depends on `-core`; `:app` depends on `-2014` (gets `-core` transitively). All tests green + APK assembles. Establishes the seam iOS proved at step 23 *before* the 2014 engine fully grows in. *(iOS 23 architecture, applied up front.)*

### UI shell + persistence

- [x] **A4. UI shell** — character list → character sheet (read-only), driven by a `CharacterStore` (`ViewModel` + `StateFlow`) seeded with 3 in-memory demo 5e characters. Navigation Compose graph (roster → sheet → journal). `CharacterSheetScreen` dispatches the body by `rulesetId` (hand-written `when` → registry when a 2nd ruleset lands); `DnD5eSheetView` shows Identity / Vitals / Abilities read off `DnD5ePayload`. Journal screen is an empty state (real journal arrives with campaigns at A7). Modest ink-on-parchment theme wiring the bundled Cinzel/EB Garamond fonts — the hi-fi codex is A7e. `:app:assembleDebug` green. *(iOS 4.)*
- [x] **A5. Local persistence (Room)** — `:data` module. `PersistentCharacter` entity (payload as `Ruleset`-encoded JSON, phase flattened to a nullable `inCampaignId`), `CharacterDao` (Flow-based), `Nat20Database` (v1, schema exported to `data/schemas`). `CharacterCodec` bridges domain `Character` ↔ row, resolving the ruleset via a new `:domain` `RulesetRegistry` so `:data` stays ruleset-agnostic. **`CharacterRepository`** interface + `RoomCharacterRepository` wrap the data layer — the sync seam; the store/UI depend only on the interface, never Room (`Nat20Data` factory keeps Room inside `:data`). `CharacterStore` now projects the repository Flow into a `StateFlow` (UI unchanged from A4). Manual-DI `AppContainer`/`Nat20Application`; **demo seed is `BuildConfig.DEBUG`-only** on first launch (release starts empty → onboarding A12). Codec round-trip unit tests. *(iOS 5a; characters only — campaign-log persistence lands with the `Campaign` type at A7a. No CloudKit equivalent — Android is local-only.)*
- [x] **A6. Create / edit / delete flows** — `CharacterStore.save`/`delete` over the repository. New-Character FAB on the roster → minimal 5e `DnD5eEditorScreen` (name / race / class+level / max HP / six ability steppers); Edit action on the sheet reuses the same form prefilled; swipe-to-delete with an `AlertDialog` confirmation. Routes resolve characters from the observed roster list, so create/edit/delete reflect immediately. *(iOS 6. The form is intentionally minimal — the parchment creation **wizard** is A8, which replaces it. PDF export deferred to a later step.)*

### Campaigns + in-play

- [x] **A7a. Campaign data model + lifecycle** — logs live on `Campaign`, not the character. `Campaign` value type + `Campaign.apply`/`end` (immutable — returns a `CampaignApplyResult`), gating on active + `CharacterPhase.InCampaign(id)` + ruleset match (`CampaignError`). Persistence: `PersistentCampaign` (snapshots + log as JSON envelopes), `CampaignDao`, `CampaignCodec` (reuses `CharacterCodec`'s envelope, encodes events via the ruleset), Room **v1→v2 additive migration** (campaigns table, copied from the exported schema). `CampaignRepository` + Room impl; `Nat20Data.create` now returns both repos off one DB. `CharacterStore.startCampaign`/`endCampaign`/`applyIntent` coordinate both stores + flip the phase. Domain + codec unit tests. *(iOS 7a. UI is A7b.)*
- [x] **A7b. In-campaign UI** — phase-aware sheet chrome (building → Edit + Start; in-campaign → Actions + Journal) + active-campaign banner with End. Start-campaign dialog → `startCampaign`; End confirmation → `endCampaign`. `JournalScreen` renders the active campaign's log newest-first (reads `displaySummary`) with an empty state. `DnD5eActionsSheet` (ModalBottomSheet): Damage / Heal / Temp HP / Note tiles → amount/note pickers → `applyIntent`, closing the play loop (intent → character mutation + journal entry, persisted). New `AddNote` play intent in the 2014 engine (+ test). *(iOS 7b. Deferred: editable journal entries (iOS 26), rename, derived sessions (A7d). The hi-fi parchment Actions sheet is A7e.)*
- [x] **A7c. Past Adventures** — build-phase "Past" action (shown only when ≥1 ended campaign) → `PastAdventuresScreen`: ended campaigns newest-ended first, each row showing name, character + primary class + level arc (from start/end snapshots), and ended date. Tapping opens that campaign's journal read-only — the journal route is now keyed by campaign id, serving both the active journal and past ones. *(iOS 7c. Party omitted — not modelled until A23.)*
- [x] **A7d. AI chronicles** — on-device per-session narrative prose. Domain: `CampaignSession.derive` (groups the log into sessions by a 4h gap) + `SessionChronicle` + `Campaign.chronicleParagraphs` (persisted via DB **v2→v3** migration + codec). `ChronicleService` builds the prompt (third-person past-tense, every event in order, nothing invented, no game terms) and calls a `ChronicleGenerator` seam; `CharacterStore.generateChronicles` generates missing sessions oldest-first (prior paragraphs for continuity) with a `generatingChronicles` flag. Journal groups entries by session and renders the chronicle (or a "Chronicling…" indicator) above each. **Availability-gated** via the single `isChronicleAvailable` flag. *(iOS 7d. The actual **Gemini Nano** binding (`GeminiNanoChronicleGenerator`) is hardware-gated — inert (`isAvailable=false`) until run on a supported device with the AICore SDK added; pipeline, persistence, gated UI, and graceful degradation are complete and prose appears automatically once a real model is bound. Sanitizer + refine-pass deferred.)*
- [x] **A7e. Codex shell** — persistent `CodexShellView` (replaces `DnD5eSheetView`): hero header (gold drop-cap + name + race/class/level) over a swipeable `HorizontalPager` with a 6-tab bottom bar (Stats / Skills / Combat / Spells / Items / Lore). Pages render what the payload supports — **Stats** (ability medallions, proficiency, passive perception, saves), **Skills** (18 SRD skills × ability mod), **Combat** (HP, base AC, initiative, speed, hit dice), **Lore** (identity); **Spells/Items** are empty states until A10. Sits inside the phase-aware chrome from A7b (banner + Actions + Journal). *(iOS 7e. Deferred/iterative: hi-fi parchment polish (textured ground, dark variant), skill/save proficiency dots (need A8), Spell Library + hero motto.)*
- [~] **A7f. Play mechanics** — wire every action to a real intent + journal entry. **Done — in-campaign journaling:** `CharacterStore.applyIntent` is now **phase-aware** (nullable campaign) — every codex action routes through `Campaign.apply` when in a campaign (so casting, using items, spending Ki/pools, using features, resting, adjusting coins, spending hit dice all journal) and is a direct unlogged edit while building; invalid/gated intents are swallowed. The whole codex (Items/Spells/Combat/Resources tabs + Actions sheet) shares one `onApplyIntent`; non-journal edits (equip toggle, learn/forget cantrips & known spells) stay on a direct `onSave`. **Done — Inspiration** (`hasInspiration` flag + `SetInspiration` intent/event + Combat-tab Grant/Use toggle) and a **campaign opening line** (seeded into the journal on `startCampaign`). The slot-accuracy engine + the bulk of the play intents (`UseItem`/`AdjustCoin`/`CastSpell`/`PrepareSpell`/`UnprepareSpell`/`ExpendSpellSlot`/`ShortRest`/`LongRest`/`SpendHitDie`/`SpendResource`/`UseClassFeature`) were built in A10 and now journal. **Done — Death saves:** `DeathSaves` (clamped 3-success/3-failure tracker, `isStable`/`isDead`/`isCleared`) in `-core`; payload `deathSaves` + `isDying`/`isDead` helpers; `MarkDeathSave(success/failure/clear)` intent + event (rejects past stable/dead/cleared); `LongRest` clears them and `Heal` off 0 HP auto-clears (RAW revive, flagged in the event); Combat-tab Death Saves tile (success/failure pips + Success/Failure/Clear buttons, shown while downed) with nat-20/nat-1 guidance text (no die roller yet — A16). **Done — Conditions:** `Condition` enum (the 14 standard 5e conditions with effect blurbs) + `Exhaustion` (2014 per-level table, level 6 = death) in `-core`; payload `activeConditions` (free-form strings → standard + homebrew) + `exhaustionLevel` (0–6) + `isDead` now also true at exhaustion 6; `ApplyCondition`/`ClearCondition` (case-insensitive dedupe) + `AdjustExhaustion` intents/events; `LongRest` sheds one exhaustion level; Combat-tab **Conditions** section (active chips tap-to-clear, exhaustion ± stepper with effect text, add-picker of the 14 standard + a custom field). **Remaining (A7f-2):** concentration (track + auto-break on damage — pairs with A17 effects) and attack rolls (overlaps A15 initiative/checks + the A16 die-roll primitive). *(iOS 7f.)*

### Content + character creation

- [x] **A8. Multi-step character creation wizard** — **Foundation:** catalogue models (`Race`/`CharacterClass`/`Background`/`Skill`) over the bundled SRD JSON via `DnD5eCatalog`; `SkillCatalog` (18 skills); `DnD5ePayload` gains `background`/`selectedSkills`/`alignment`. **Wizard:** `DnD5eWizardScreen` (Name → Race → Class+level → Background → Abilities → Skills → Review) replaces the A6 form — pick-cards from the catalogues, base scores + auto racial bonus, class skill-quota chips (background skills auto-granted), HP computed from hit die + CON, materialises a `Character`. **Codex now shows real proficiency:** Skills / Saves / Passive Perception read `selectedSkills` + the class's saving throws with proficiency dots. Demo roster enriched. Catalogue-loading tests. *(iOS 8. Deferred: Manner/alignment, Spells step, Advancements/high-level (A11), subclass picker, point-buy/standard-array, full parchment chrome.)*
- [~] **A9. Settings + Reference codex** — **Done:** `SettingsScreen` (gear on the roster) with **live light/dark/system appearance** backed by `AppSettings` (SharedPreferences), which also lands the **candlelit dark theme** A7e deferred (`Nat20Theme(darkTheme)` threaded from `MainActivity`); **Credits & Licenses** carrying the SRD 5.1 (CC BY 4.0), 5e-bits/5e-database, and typeface (SIL OFL 1.1) attributions verbatim. *(iOS 9. Deferred: narration style (needs chronicle styles), and the ruleset-tabbed Reference codex — Spell Library / Item Catalog / Monster Codex — which needs the spell/item/monster catalogues loaded (A10+).)*
- [~] **A10. Expand 5e content** *(taken in slices)* — **Done — Spell Library:** `Spell` model decoding the raw 5e-database SRD JSON (319 spells) via `DnD5eCatalog.spellLibrary`; `SpellLibraryScreen` (search + level filter + expandable rows) reachable from **Settings → Reference** and the codex **Spells tab** (caster-aware Browse button). This also fills A9's Reference section for spells. **Done — Inventory & equipment:** `ItemKind`/`WeaponProperties`/`ArmorProperties`/`InventoryItem` models + `Coin` (in `-core`); `WeaponCatalogueEntry`/`ArmorCatalogueEntry`/`GearCatalogueEntry` decode the bundled `Inventory/*.json` via `DnD5eCatalog.weapons`/`armor`/`gear` with `makeItem()`. Payload gains `inventory` + `coins`; **real AC** via `ArmorClassCalculator` (one equipped armor → base + DEX-cap rule by light/medium/heavy, shield + item `acBonus` stacking) surfaced with a breakdown on the **Combat tab**. Logged-play intents `AcquireItem`/`DropItem`/`UseItem`/`AdjustCoin` (+ events + codec) with JUnit5 coverage. Editable **Items tab** (coins, equipped, grouped inventory; add-from-catalogue, equip toggle, drop, use, adjust coins) — equip is a direct payload toggle, add/drop/use/coin reuse the intents; demo roster seeded with gear. (Active-effects AC riders — Mage Armor, Unarmored Defense, Shield spell — wait on A17; scroll/charged-wondrous properties too.) **Done — Spells (known/prepared + slot tables):** `SpellSlotTable` + `CastingProgression` in `-core` (full/half/third/warlock tables verbatim from PHB, prepared limits, spellcasting ability); `Spellcasting` in `-2014` for the `ClassEntry`/multiclass math (combined caster level → full table, EK/AT third-casting via subclass, warlock pact slots tracked separately + merged for display). Payload gains `cantripsKnown`/`spellsKnown`/`preparedSpells`/`currentSpellSlots`/`currentPactSlots` + derived `maxSpellSlots`/`pactSlotLevel`/`totalCurrent`/`totalMax`/`castableSpellIDs`. Intents `CastSpell` (pact-first drain, ritual/scroll = no slot, upcast), `PrepareSpell`/`UnprepareSpell`, `ExpendSpellSlot`, `ShortRest` (pact) / `LongRest` (HP+temp+all slots) — all with JUnit5 coverage. Editable **Spells tab**: slot tracker (pips + Use + short/long rest), cantrips, prepared/known list, cast in place, add-from-library; casters seed full slots at creation + in the demo roster. **Done — Hit dice:** payload `hitDiceSpent` with derived `currentHitDice`/`maxHitDice` (= level); `SpendHitDie` intent (heal by a passed-in rolled amount, clamped) + `HitDieSpentEvent`; `LongRest` now regains half the spent dice (min 1); Combat tab shows the available/max pool. **Done — Class features + pool resources (full iOS parity):** `-core` `ClassResourceCatalog` (point pools: Ki = monk level, Sorcery Points = sorcerer level, Lay on Hands = paladin×5) + `ClassFeatureCatalog` (use-counters: Rage 2→3→4→5→6→∞, Action Surge 1→2, Second Wind, Bardic Inspiration = CHA mod, Channel Divinity 1→2→3) with `FeatureRecovery` + scaling helpers (Bardic die, Rage damage). Payload gains `resourcePools` + `classFeatureUses` (missing key = full); `-2014` accessors resolve them per-character (`availableResourcePools`/`availableClassFeatures`, injecting Bardic Inspiration's CHA cap + L1-4/L5+ recovery split). Intents `SpendResource` (point pools, overdraw-guarded) + `UseClassFeature` (counter decrement, ∞ at L20); `ShortRest` now also resets short-rest features/pools (Ki, Action Surge, …), `LongRest` wipes both. Combat tab gains a **Class Resources** section (pools with a spend stepper, feature counters with Use) + a **universal Rest section** (so non-casters can rest too; the Spells tab's slot tracker now points there). JUnit5 coverage for every formula, intent, and rest reset. **Remaining:** item usage + spell-cast + rests + hit-die spend + resource-spend wired into the in-campaign Actions sheet (A7f); creation-time spell picks + per-level learning (A11); concentration + the on-use `ActiveEffect`s riding class features/spells, incl. Rage damage bonus and at-will/`OTHER`-recovery features like Reckless Attack / Hunter's Mark (A17). (See the iOS README's "5e rules — coverage gaps".) *(iOS 10.)*
- [~] **A11. Level-up wizard + high-level creation** — **Done — Level-up wizard:** `LevelUp` extended with `subclass` + `abilityIncreases` (+ catalogue-resolved `className`); applies the ASI (capped at 20), records the subclass on the class line, and grants newly-unlocked spell slots while preserving spent ones; `LeveledUpEvent` carries the subclass/ASI for the journal. `LevelUpMath.grantsAbilityScoreImprovement` (4/8/12/16/19 + Fighter 6/14 + Rogue 10); catalogue gains `Subclass` + `CharacterClass.subclasses`/`subclassLevel` (decoded from the SRD class JSON). UI: a **Level Up** button on the Stats tab opens `LevelUpWizard` (hosted in the codex shell) — pick class / multiclass → HP **average or rolled (A16 die primitive)** → subclass (at the subclass level) → Ability Score Improvement (+2 one / +1 two) → apply; journals in-campaign. JUnit5 for ASI cadence, score-capping, subclass recording, slot-grant-on-level, subclass catalogue, and the extended event codec. **Remaining:** ASI-vs-**feat** choice (needs a feats catalogue), the deep class catalogues (fighting styles, metamagic, invocations, pact boons, expertise), in-wizard spell picks (today on the Spells tab), and the high-level-creation "Advancements" step for characters made above level 1. *(iOS 11.)*
- [ ] **A12. First-run onboarding** — a parchment first-run flow shown once on launch (Welcome / Characters / Campaigns / The Seal), AI-aware copy where on-device AI is real, completion persisted. Pairs with the debug-only demo seed (A5) so a fresh install lands on onboarding → character creation. *(iOS 25.)*

### Platform polish + monetisation

- [ ] **A13. Tablet / large-screen layouts** — width-aware readable caps; multi-pane where it adds value (codex + actions side-by-side in landscape). *(iOS 12a/12b.)*
- [ ] **A14. Free tier + paywall** — Google Play Billing. One character free; additional characters via a one-time IAP. Mirror the iOS gating (`isPatron` single source of truth; roster CTA flips to "Upgrade" at the free limit). *(iOS 13.)*
- [~] **A15. Combat actions audit + Initiative** — **Done — Attacks:** `AttackMath` (-2014) derives a weapon's attack bonuses (STR / DEX-for-ranged / better-of for finesse + proficiency) and damage spec; `MakeAttack` intent + `AttackEvent` (logs weapon, roll total, hit/miss/crit, damage) + `RollSpec.parse`/`critDoubled` (-core); an **AttackSheet** flow (pick equipped weapon → roll attack via the A16 primitive → outcome auto-suggested from nat-20/1 → roll damage, doubled on crit → log), reachable from the Combat tab. **Done — Initiative:** payload `initiative` + `initiativeBonus` (DEX); `SetInitiative` intent + event; Combat-tab roll/reroll/clear. **Done — checks** (skill/save/ability are tap-to-roll via A16, landed there). All in-campaign actions journal through the shared `onApplyIntent`. **Remaining:** class/feat damage riders (Rage, Sneak Attack, Divine Smite — A17 effects), ammunition tracking for ranged, slot-only expenditure tile + pass/fail-judged checks in the journal, and concentration auto-break on damage (A17). *(iOS 14.)*
- [~] **A16. Die-roll animation primitive** — **Done:** the pure dice model + roller in `-core` (`RollSpec`/`Keep`, `RollBonus`, `RollResult` with kept-dice/total/crit-fumble, `DiceRoller` with an **injectable seedable `Random`** — better than iOS for tests); the Compose `RollResultView` (tumbling dice on an easeOutCubic curve → settle → bonus chips stream in → total ticker → natural-20/1 flourish → haptics) with an Advantage/Normal/Disadvantage selector, plus a `RollDialog` wrapper. **Consumers wired:** death saves (atomic `RollDeathSave(d20)` intent — nat-20 revive at 1 HP, nat-1 two failures, 10+ success, else failure — journals in-campaign), hit-die spend (1dX + CON → `SpendHitDie`), and tap-to-roll skill / ability / saving-throw checks on the Stats & Skills tabs (transient, with the advantage toggle). JUnit5 for the roll math + `RollDeathSave`. **Remaining:** attack & damage rolls (need the weapon/crit-double machinery — pairs with A15) and the HP-roll-on-level-up consumer (A11); deeper flourish (crit burst particles) and Halfling Lucky reroll are polish. *(iOS 15.)*

### Deeper rules + later rulesets

- [ ] **A17. Active effects** — unified in-play modifier model (spells/items/features as typed effects with durations + sources, surfaced as coloured chips with concrete impact). Auto-apply on cast/use/feature, auto-cancel on concentration-end / long-rest. *(iOS 17.)*
- [ ] **A18. Familiars + summoned creatures** — secondary entities tied to a character (Find Familiar/Steed, Conjure Animals, Summon X family, Beast Master companion), surfaced as statblock cards + a homebrew creature library. *(iOS 18.)*
- [ ] **A19. Race-trait mechanical auto-apply + optional content sources** — race traits that fire their rule effect (Lucky, Savage Attacks, Relentless Endurance, innate resistances, save advantage), plus per-character supplement toggles (`enabledSources`), parameterized feats, subclasses, Custom Origin/Lineage. *(iOS 20–21.)*
- [ ] **A20. AI-assisted character creation** — "describe your character" → on-device LLM produces a *concept*, deterministic Kotlin fills the mechanics. Gated behind the same on-device-AI availability flag as chronicles. *(iOS 22.)*
- [ ] **A21. D&D 5e (2024) ruleset** — second 5e edition as a sibling ruleset (`rulesetID "dnd-5e-2024"`) in a new `:ruleset-dnd5e-2024` module on `:ruleset-dnd5e-core`. Ability scores move from species to **background**; species carry traits only; tiered feats; Weapon Mastery; subclasses at level 3; unified Arcane/Divine/Primal spell lists. Existing 2014 characters stay bound to 2014 forever (no cross-edition migration). Content from SRD 5.2 (CC BY 4.0). *(iOS 23 — this is where the `-core` seam pays off.)*
- [ ] **A22. Pathfinder 2e (Remaster) ruleset** — first non-D&D system (`rulesetID "pf2e-remaster"`) in `:ruleset-pf2e` on its own `:ruleset-pf2e-core`. Five-rank proficiency ladder, four degrees of success, valued conditions, traditions/heightening, Hero Points + dying/wounded. *Full* ORC content is the target (legally sourceable via AoN). Three-action economy modelled as static action-cost data, **not** a live turn tracker (companion-app scope). Build sub-steps mirror iOS 24's: core+payload → derived stats → ancestries/creation → classes → spells → feats → play intents → equipment → AI creation. *(iOS 24.)*

### Cross-platform + far-later phases

- [ ] **A23. Party mode — shared journals (cross-platform)** — players in the same campaign share third-person journal entries; join by **code** (no accounts). **Cross-platform from day one** (iOS + Android players in one campaign) — this is the explicit reason iOS dropped CloudKit as the transport. A thin shared **party plane** (Supabase recommended: anonymous auth + RLS + realtime + receipt-validating edge function; Swift + Kotlin + JS SDKs) holds campaigns/memberships/shared entries, kept *separate* from the local-first private plane. Each entry carries `inputPayload` (portable structured event) + `bodyBasic` (deterministic third-person) + `bodyNarrated` (on-device-AI polish, backfilled by any capable teammate's device — Android Gemini Nano can narrate an iOS member's entries and vice-versa). **Read-gated** behind the same IAP as A14. *(iOS 16 — unbuilt on both platforms; needs the shared backend designed jointly. Do not get ahead of the reference app.)*
- [ ] **A24. Active-effects / summon cross-character propagation, PDF export, and the iOS "coverage gaps" long tail** — track against the iOS README as those land there. *(iOS open follow-ups.)*

## Open decisions

- **Multi-ruleset module seam — resolved (anticipate now).** Mirror iOS's `DnD5eCore` / version-package split from the start (`:ruleset-dnd5e-core` + `:ruleset-dnd5e-2014`), rather than extracting later. The 2024 / PF2e modules get scaffolded at their build steps (A21/A22), not as empty modules now.
- **Kotlin Multiplatform (deferred).** Parallel Kotlin port over a KMP shared domain to keep the shipping iOS app untouched and Android idiomatic. Revisit if logic drift between platforms becomes painful — the natural seam would be a KMP module for the domain + ruleset cores.
- **Sync backend (deferred for character data; required for party mode).** Character data is local-only via Room behind `CharacterRepository`. **Party mode (A23) is the exception** — it needs a thin shared backend (Supabase recommended) and is cross-platform with iOS, so it must be designed jointly with the iOS side when that step is taken.
- **Min SDK + on-device-AI device coverage.** `minSdk = 26` is pinned. Confirm which devices actually expose Gemini Nano / AICore; AI features are availability-gated, so unsupported devices fall back gracefully (no chronicle / no AI creation, never a broken affordance).
- **Monetisation parity.** Keep the Play Billing model aligned with the iOS StoreKit model (one free character + IAP unlock); the same purchase also read-unlocks party mode.

## Reference

- iOS reference app + canonical roadmap: [`../nat20-ios/README.md`](../nat20-ios/README.md)
- SRD JSON to copy: under each iOS ruleset package's `Sources/<Package>/Resources/`
