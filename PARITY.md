# iOS ↔ Android screen parity audit

Goal: every Android screen looks and is structured like its iOS counterpart.
Process per screen: code-level diff → apply fixes → Bartek eyeballs both devices → tick.
Started 2026-07-13. Walk order = navigation order.

Legend: `[ ]` not audited · `[~]` audited, fixes in progress · `[x]` fixed + visually confirmed

## App shell & home

- [~] 1. Splash — iOS `Nat20App.swift` (`Nat20SplashView`, ~1.2s fade) ↔ `ui/theme/Brand.kt` (`SplashView`)
  - Built 2026-07-13: ported `Nat20MarkD20Iso` / `Nat20Wordmark` / `Nat20Lockup` to Compose (`Brand.kt`), splash overlay in `MainActivity` (1.2s hold, 0.4s ease-out fade, onboarding/roster ready behind it). Light = texture + fox-marks, dark = candle-glow. Awaiting visual confirm.
- [~] 2. Roster / home — `CharacterIndexView.swift` ↔ `ui/roster/RosterScreen.kt`
  - Fixed 2026-07-13: dashed CTA border, pinned-bar 3-stop gradient + top hairline, DropCap 34sp + cream inner-glow ring, chevron 18dp top-aligned, empty-tile 40dp top gap, gear 20dp, list bottom padding. Awaiting visual confirm.
  - Resolved 2026-07-14 (with #23): portrait-in-card now lights up — `CharacterIndexCard` renders the stored portrait via the shared `CharacterPortraitFrame`, falling back to `DropCapSquare` when there's none (portrait persistence landed with #23's Room v5→v6). Android-only swipe-to-delete stays until #40 (Character Settings) provides the iOS delete path.
- [~] 3. Onboarding (4 pages) — `Onboarding/OnboardingView.swift` ↔ `ui/onboarding/OnboardingScreen.kt`
  - Restyled 2026-07-13: was generic Material (Button/headlineSmall/circle dots); now parchment chrome — SKIP pill, ruled Cinzel label, Cormorant italic titles, EB Garamond body, diamond dots, `PrimaryActionButton` (new shared port in `theme/Controls.kt`), and iOS vignettes (Welcome lockup, tinted die.png copied from iOS assets, Session-II journal plate w/ burst+sparkle glyphs, sealed sheet + wax seal). Copy was already identical. Awaiting visual confirm.

## Settings & friends

- [~] 4. Settings — `Settings/SettingsView.swift` ↔ `ui/settings/SettingsScreen.kt`
  - Fixed 2026-07-13: shared `SectionLabel`/pills to iOS caption2 sizing (11sp, pill v-padding 7), tile chevrons 16dp, patron-badge icon 20dp, bottom padding 24. Removed Android-only "Replay introduction" tile; added iOS-parity DEBUG-only Developer section ("Always Show Onboarding" toggle + `AppSettings.alwaysShowOnboarding` + per-launch gate in `MainActivity`). Awaiting visual confirm.
  - Deferred: "Custom Creatures" reference tile lands with #44 (needs the whole homebrew feature).
  - Resolved 2026-07-13: Android-only "Actions (PF2e)" reference tile + `PathfinderActionsScreen` **removed** per Bartek (exact parity; recoverable from git history). The `PfActions` catalogue + tests stay in `:ruleset-pf2e` (inert data, harmless).
- [~] 5. Patron paywall — `Patron/PatronView.swift` ↔ `ui/patron/PatronScreen.kt`
  - Rewritten 2026-07-13: was generic Material (TopAppBar/Card/Button); now iOS chrome — CLOSE/UNLOCK top nav, ornamental divider, d20 crest ("PATRON" eyebrow, Cormorant display title, IM Fell tagline), parchment perk tile w/ drawn three-person glyph, accent pledge button (Cinzel small-caps, accentDeep outline, in-flight spinner) + RESTORE PURCHASE + footnote. Auto-dismiss on unlock kept. "Google account" wording kept (platform-correct vs iOS "Apple Account"). Awaiting visual confirm.
- [~] 6. Credits — `Settings/CreditsView.swift` ↔ `ui/settings/CreditsScreen.kt`
  - Fixed 2026-07-13: section labels now use shared `SectionLabel` (fading rule, iOS `PickerSection` paddings), card titles 18sp, back chevron 18dp. Attribution copy verified verbatim. "↗" text glyph stands in for iOS `arrow.up.right.square`. Awaiting visual confirm.

## Character creation

- [~] 7. Creation entry — iOS: unified wizard w/ Ruleset step (`CharacterCreationWizard.swift`) · Android: EditionChooser AlertDialog in `NatApp.kt` — **structural**
  - Built 2026-07-13: unified `CreationWizardScreen` (Ruleset step w/ selection-dot edition tiles, indicator previews 1→8 steps once an edition is picked) + shared `EditorShell` chrome in new `ui/editor/WizardShell.kt` (back circle + kicker/title nav row, diamond step rail w/ tap-back jumps, hairline divider, pinned gradient footer w/ capsule Cancel + Continue/Create/Save buttons, `WizardStepSection`). EditionChooser AlertDialog + the three per-edition create routes removed; single "create" route. All three wizards re-hosted on `EditorShell` with `stepOffset = 1` + `onExitFirstStep` (back/jump from their first step returns to the Ruleset step); 2014 edit route unchanged (stepOffset 0). Step bodies untouched — that's items 8–10. Awaiting visual confirm.
  - Deferred: Content Sources disclosure under the selected 2014 row (needs the SourceCatalog port); per-edition draft retention when switching editions mid-flow (iOS keeps three drafts — Android re-entering an edition resets it); AI-describe card seam (A20).
- [~] 8. 2014 wizard — `DnD5eEditorView.swift` + steps ↔ `ui/editor/DnD5eWizardScreen.kt` (iOS extra steps: Equipment, Manner, AI-describe card)
  - Built 2026-07-13: **Equipment + Manner steps** added and steps reordered to iOS (Name → Race → Class → Background → Abilities → Advancements(cond) → Skills → Equipment → Spells(cond) → Manner → Review). Engine: `DnD5ePayload` +personality/ideals/bonds/flaws/backstory; `CharacterClass.starterEquipment` (+`StarterEquipmentEntry`) + `features`, `Race` +languages/traits/ages, `Background` +tools/languageCount/equipment/feature decoded from the bundled SRD JSON; new `StartingEquipment.seed(classId, backgroundId)` (weapon→armor→gear resolution, first weapon/armor/shield auto-equipped, background free-text gear lines, equipped-dagger fallback) with JUnit5 coverage (per-class parameterized seed table, fighter kit, rogue quantities, fallback) + payload round-trip extended. UI: new `ui/editor/WizardAtoms.kt` (field label, stat cell, level stepper, segmented, parchment text field, chip pickers + multi-select w/ quota, selection dot, quote block, detail card, sub-section card) and **every step body restyled** onto them (level stepper + tier hint on Name per iOS; Character Level banner + class detail card w/ features-by-level quote blocks; race/background detail cards w/ stat-cell grids, equipment diamond grid, feature quote; abilities tile rows + final-scores grid w/ accentGold race badges; locked background skill chips + quota'd class chips; spell rows w/ selection dot, meta line, V/S/M badges; 3×3 alignment grid + manner/backstory fields; Review hero drop-cap + per-section pencil-jumps w/ editing-from-review "Done" footer mode). Equipment step seeds on first arrival (edit mode keeps existing inventory; class change re-seeds only when no real inventory), grouped by kind w/ equip toggles, Add Item search + quantity dialog, Reset to starter kit. LorePage Backstory card now shows alignment + manner quotes + backstory prose. Shared `IdentityStep` restyled in place (2024/PF2e wizards pick it up). Awaiting visual confirm.
  - Divergence: **Fighting Style stays a separate step** (after Skills). It can't move into Advancements — a level-1 Fighter grants a style but has no Advancements step (no subclass/ASI yet), so folding it in would drop the pick at level 1.
  - Deferred: portrait picker (→ #23), custom races + variant-human bonus skills (→ #11), multiclass config UI (Android is single-class), ability-score methods Array/Point-Buy/Roll (manual steppers kept), Tasha's custom origin + optional class features, full custom-item form in Add Item (catalogue search + quantity only), spell detail sheets in-wizard, AI-describe card seam (A20).
- [~] 9. 2024 wizard — `DnD5e2024EditorView.swift` ↔ `ui/editor/DnD5e2024WizardScreen.kt` (iOS extra steps: Weapon Mastery, Fighting Style, Armor, Subclass, Spells)
  - Built 2026-07-13: **five new steps + iOS step order + full restyle.** Steps now Name → Species → Class → Subclass(level 3+) → Background → Skills → Abilities → Advancements(ASI levels) → Weapon Mastery(mastery slots) → Fighting Style(Fighter 1 / Paladin·Ranger 2) → Spells(casters w/ choices) → Armor → Review (Skills before Abilities per iOS; subclass moved out of Advancements into its own step). Engine: `CharacterClass2024` +`armorProficiencies`/`hasShieldProficiency` (2024 PHB values for all 12 classes); new `Creation2024.kt` — `FightingStyle2024.grantLevel` + `Spellcasting2024Creation.cantripsKnown`/`leveledSpellsKnown` (per-class quotas, +1 cantrip at 4/10, full-caster vs half-caster/warlock leveled scaling) — w/ parameterized JUnit5 tables (module now 105 test cases). UI: Background step restructured — chips + detail card + **ASI allocator** (delta stepper per ability option, +3 total as +2/+1 or +1/+1/+1, default seed +2 first/+1 second, `increases` map replaces the old asiMode/plus2/plus1 Abilities-step state) + Origin Feat quote card; Weapon Mastery multi-select rows ("{weapon} — {mastery}", summary + category·damage detail, exact-slot gate, dim at cap); Fighting Style single-select rows over `Feats2024` FIGHTING_STYLE (tap again clears); Spells step (class-filtered SRD 5.2 cantrips + 1st-level, quota-capped at catalogue, always-passable gate, prepared → `preparedSpells[classId]`); Armor step (live AC banner via `ArmorClass2024` preview payload so Unarmored Defense/Defense fold, shield toggle gated on proficiency, None + catalogue rows w/ dex notes, non-proficient dimmed 0.4 + disabled, fold validates proficiency); Advancements restyled (LEVEL N card, Ability Scores/Feat segmented, +2 delta steppers or General-feat rows + half-feat +1). Every body restyled onto WizardAtoms (`SelectableRow2024`, species/class/subclass/background detail cards, abilities tiles + final-scores grid w/ accentGold background badges, locked background skill chips); class switch clears subclass/masteries/style/spells/skills/armor/shield; Review rebuilt (hero drop-cap, per-section pencil-jumps for active steps only, editing-from-review "Done" footer; review atoms ReviewSectionHeader/ReviewLine/ReviewDropCap/GrantedSkillChip promoted to `WizardAtoms.kt` and shared with the 2014 wizard). Dead Material3 locals (Pick2024/Class2024/Stepper2024/StepLabel2024/Review2024) deleted. Awaiting visual confirm.
  - Deferred: ability-score methods Array/Point-Buy/Roll (manual steppers kept, like 2014); portrait picker; spell detail sheets in-wizard; subclass/class descriptions + feature lists on detail cards (2024 seed catalogue carries names only — needs the catalogue-depth follow-up); edit-mode re-entry (2024 wizard is create-only, matching pre-existing behavior); AI-describe card seam (A20).
- [~] 10. PF2e wizard — `PathfinderEditorView.swift` ↔ `ui/editor/PathfinderWizardScreen.kt` (iOS extra steps: Subclass, Spells, Feats, Advancement, Equipment)
  - Built 2026-07-13: **five new steps + level stepper + full restyle.** Steps now Name → Ancestry(+heritage) → Background → Class(+key ability + **starting-level stepper**, note when >1) → Subclass(classes with one) → Abilities → Skills → Spells(casters) → Feats → Advancement(level >1) → Equipment → Review; kicker counts filtered steps. Engine (`:ruleset-pf2e`): `PfSubclass` (id/name/summary/optional `grantedSkill`) + `PfClass.subclassLabel`/`subclasses`/`hasSubclass` filled from the iOS catalogue for the 7 subclassed seed classes (rogue Racket, cleric Doctrine, wizard School, sorcerer Bloodline w/ granted skills, ranger Hunter's Edge, champion Cause, bard Muse; fighter none); `PathfinderPayload.subclass`; `Wealth.startingWealthGP` (GM Core 1–20 table) + `kitCostGp`/`kitBulk`; `PfArmor`/`PfShield`/`PfWeapon` +`priceGp`/`bulk` (iOS values; RAW for the two Android-only armors); `FeatSlotKey(level, type)`; `PfLevelUp` maths extracted to shared `PfLevelMath.advance` (intent + builder share one copy); `PathfinderBuilder.Choices` +targetLevel/subclass/cantrips/rank1Spells/ancestryFeat/classFeat/advancementBoosts/skillIncreases/advancementFeats/armor/shield/weapons; `build()` folds subclass + granted skill, filters spell picks to tradition + caps at day-one quotas, writes feats in earn order (ancestry → class → advancement by level), drops non-proficient armor/weapons, seeds `coins` at target-level wealth, then replays levels 2..target through `PfLevelMath` (boosts at 5/10/15/20, capped skill increases, class prof bumps, slot top-ups). 10 new JUnit5 cases incl. a build-vs-hand-leveled-chain equality test (`:ruleset-pf2e` 45, `:ruleset-pf2e-core` 21). UI: every inlined Material3 `when` arm extracted to a step composable on the WizardAtoms (chips pickers, detail cards w/ stat cells, boost multi-select chips w/ per-group no-dup + ancestry-fixed-boost block, six-tile score preview grid, granted-skill chips, quota'd class-skill chips, capped Cantrips/1st-Rank groups w/ selected-spell quote blocks (never gates), Ancestry/Class feat groups (gate only when options exist), per-gap-level Advancement cards (4-boost picker, capped skill-increase chips vs evolving ranks, feat slots per FeatSlots clocks w/ "not catalogued" placeholders), Equipment step w/ live ARMOR CLASS banner via a preview build, armor/shield stat lines, per-weapon Strike quote lines, Load & Cost advisory (Bulk + encumbered badge + kit/wealth/remaining, danger when negative, never gates)); class switch clears subclass/skills/spells/class feat/adv feats/equipment; Review rebuilt (hero drop-cap, Heritage-Ancestry + "Subclass Class N" lines, six ability tiles, Vitals line HP·AC·Perception·Class DC + "Casts <tradition> spells", dot-joined feats, Advancement jump when level >1; pencil-jumps + editing-from-review "Done" footer). Dead locals PickRow/BoostGroup/PfLabel/PfReviewLine deleted. Sheet Lore Identity row now shows "Subclass Class N". Awaiting visual confirm.
  - Deferred (per port spec): manual ability-score entry toggle (boost-built only); portraits; rune editors in-wizard (runes stay on the Combat tab); monk Path to Perfection (class not in the Android seed catalogue); `PFCreationRecord` re-editability snapshot (PF2e has no edit mode on Android); witch-style subclass-resolved traditions (no such class in the Android catalogue — sorcerer bloodlines keep the class's fixed arcane tradition); heritage summaries (Android heritages are name-only slugs).
- [~] 11. Custom Race form — iOS `CustomRaceForm.swift` ↔ `ui/editor/CustomRaceForm.kt`
  - Built 2026-07-13: **homebrew races end-to-end.** A homebrew is a regular `Race` with id `"custom:"+UUID` (no separate type). Engine (`:ruleset-dnd5e-2014`): `CustomRaceLibrary` (StateFlow list, add/update/delete + `replaceAll` hydration that does NOT fire `onChange`, `isCustom` prefix gate) + `CustomRaceJson` Race↔JSON codec; `DnD5eCatalog.races` now overlays homebrews after the SRD list, name-sorted case-insensitively, always included regardless of any future source filter (8 new tests, module 315). Data: `custom_races` table (id pk, raceId, name, payloadJson, createdAt, updatedAt — thin envelope, `:data` stays ruleset-agnostic) + `CustomRaceDao`/`CustomRaceRecord`/`RoomCustomRaceRepository`; Room **v4→v5** additive migration per the summons precedent, schema 5.json exported (2 new codec tests). App: `CustomRaceSync` hydrates the library at startup then write-through-reconciles on every mutation (update-by-raceId / insert / delete); wired in `AppContainer` + `Nat20Application`. UI: full-screen parchment dialog (CANCEL · NEW RACE/EDIT RACE over "Homebrew" · spacer header; Name/Description/Size segmented + Speed 5–60·5 & Darkvision 0–120·30 steppers ("—" at 0)/six ASI steppers −2..+3 (zero entries dropped)/comma-separated Languages/prose TraitEditorCards + dashed "Add trait"; sticky cream-gradient footer with Delete-when-editing + Save gated on name). 2014 wizard Race step: picker includes homebrews (StateFlow collected → re-renders on save/delete), dashed "+ NEW RACE" capsule, detail card gets HOMEBREW badge + 28dp pencil → edit form; delete clears the wizard's raceId if it matched. Awaiting visual confirm.
  - Deferred: saved characters keep a dangling `race` id when a homebrew is deleted (iOS behaviour); portrait/age fields not captured (same as iOS — form defaults them); source "phb" default implicit (Android `Race` has no source field yet — homebrews are already source-filter-immune by construction); no live Room migration test (`:data` JVM suite has no Robolectric harness; v4→v5 SQL matches the exported 5.json verbatim); custom races surface in the 2014 wizard only (codex Lore card shows name via the same catalogue overlay, but no badge/edit there — matches spec scope).

## Character sheet — 5e 2014 codex

- [~] 12. Codex shell/chrome (hero row, campaign region, tab bar) — `CodexShellView.swift` ↔ `ui/codex/CodexShellView.kt`
  - Built 2026-07-13: **shell owns all 2014 chrome.** `CharacterSheetScreen` no longer draws Scaffold/TopAppBar/ActiveCampaignBanner for the 2014 body (2024/PF2e keep the Material Scaffold pending #24/#34); it passes callbacks and keeps hosting the dialogs (StartCampaignDialog, End confirm, DnD5eActionsSheet). Shell = TopNavRow (38dp tileStrong back circle + chevron; Diamond(5)-flanked CURRENT-TAB title Cinzel 12/tracking-5 synced with the pager; phase pill — ACT capsule w/ drawn circle-plus glyph in campaign, EDIT tileStrong capsule while building) → HeroRow (shared 58×70 rect `DropCapSquare`, promoted from RosterScreen into `ui/theme/Ornaments.kt`; name Cormorant 28 semibold italic accent; race + "Level N Class" 13 italic inkSoft) → CampaignRegion (◆ START ADVENTURING ◆ gradient r4 button + PAST ADVENTURES hairline row w/ drawn book glyph when past campaigns exist; in campaign: CampaignJournalBar w/ name 17 italic + stroked JOURNAL button) → OrnamentalDivider → pager → iOS tab bar (Diamond(8) active / 3dp dot inactive, Cinzel 11/tracking-2 labels, cream .6→.96 vertical gradient, 1dp accent@.2 top hairline, nav-bar inset). LevelUpWizard gate unchanged; pages untouched (#13–18).
  - Divergences: **End affordance** — iOS ends campaigns from campaign settings (#37); Android keeps a small italic "End" text in the journal bar (smallest divergence; move into #37 when built). **Gear omitted** — iOS's gear opens Character Settings; no Android destination yet (gear → #40 Character Settings). Awaiting visual confirm.
- [~] 13. Stats tab — `Pages/StatsPage.swift` ↔ `CodexPages.kt`
  - Restyled 2026-07-13: new codex atom vocabulary in `CodexAtoms.kt` (SectionHead fading rules, StatChip, HPBlock compact/large + gradient HPBar on the palette `track`, concentric-ring AbilityMedallion w/ dashed inner circle + compass dots, SaveCell six-across strip, CodexEffectChip, InspirationBanner, DashedNotice/DashedRule) — SectionCard-everywhere is gone. iOS order: Init/AC/Prof/Speed chip row (AC → breakdown dialog) · HP compact · ABILITIES medallions · SAVING THROWS strip · EFFECTS chips · RESOURCES pool tiles · DEFENSES (Resistance accent / Advantage accentGold; no Immune data on Android) · LANGUAGES (from race catalogue; "—" when unknown) · INSPIRATION banner. Slice C (2026-07-14) added a read-only **CONDITIONS** display strip after Effects (active + effect-imposed conditions + exhaustion badge as tinted chips; apply/clear moved to the Act sheet). Kept-interactions (deliberate Android extras — iOS is display-only): tap-to-roll medallions/saves, effect chip → detail + cancel. Android extras kept at end: Class Choices (metamagic/invocations/pact boon/expertise — iOS picks in Level Up #20) + LEVEL UP button; Feats display moved to Lore Features & Traits. Awaiting visual confirm.
- [~] 14. Skills tab — `Pages/SkillsPage.swift` ↔ `CodexPages.kt`
  - Restyled 2026-07-13: PASSIVE PERCEPTION callout (tileStrong, Cormorant 34sp value; now folds expertise multiplier + effect bonuses) · ALL SKILLS 18 rows w/ dashed [1.5,2] separators, pips (ring/filled/diamond expertise), Cormorant 15sp names (medium+ink trained), "+N effect" IM Fell accentGold rider, ABIL w32 + bonus w36 columns · pip legend. Kept: tap-to-roll every row (a deliberate Android extra — iOS rows are display-only; retained after slice C, alongside the Act sheet's Skill-check picker). Awaiting visual confirm.
- [~] 15. Combat tab — `Pages/CombatPage.swift` ↔ `CodexPages.kt`
  - Restyled 2026-07-13 to iOS order: HP large · Armor(→AC breakdown)/Init/Speed chips · HitDiceTile (130dp, tap = spend die w/ existing roll flow) + DeathSavesTile (9dp circles + rotated-square failures, always visible; tap → full manage dialog w/ Roll/Success/Failure/Clear — kept from A7f-2) · COMBAT FEATURES (fighting styles) · ATTACKS table (Weapon/Atk/Damage/Range columns from AttackMath, hairline rules). **Slice C (2026-07-14) stripped the page to display-only past Attacks:** the six inline management sections (Spell Slots expend, Inspiration setter, Active Effects manager, Conditions manager, Class Resources, Rest) were removed — every control now has an Act-sheet path (Expend a slot / Spend inspiration / Manage effects / Apply-Clear condition / Spend resource + Use class feature / Short-Long rest). Combat now matches the iOS order (HP · chips · hit dice + death saves · combat features · attacks). Kept Android extras: the **Make-an-attack** button → AttackPicker (iOS has no on-page attack button — noted), and the Init chip tap → roll (unset) / reroll-clear dialog (set); death-save tile tap → DeathSavePicker. **Companions moved out → Lore Minions.** Awaiting visual confirm.
- [~] 16. Spells tab — `Pages/SpellsPage.swift` ↔ `SpellsPage.kt`
  - Restyled 2026-07-13: non-caster empty state (drawn dashed-circle sigil + diamond + orbit dots, "No spells known.", BROWSE THE CATALOGUE) · caster: Ability/Save DC/Spell Atk chip triplet (per class when multiclass) · Browse-all button · SPELL SLOTS w56 tiles w/ remaining dots · CANTRIP capsule pills · PREPARED · n / SPELLS diamond rows + dashed empty notice. Kept-interactions (deliberate Android extras retained after slice C — the Act sheet's Cast/Expend tiles are the parity path, these are on-page conveniences): tap slot tile → ExpendSpellSlot, tap pill/row → Cast (hands to the shared CastTargetPicker)/Unprepare/Remove dialog, Add-cantrip + Prepare/Learn pickers (direct edits vs PrepareSpell intent unchanged). Payload gap: no prepared-limit accessor — head shows "PREPARED · n" without "/ limit" (defer to spellcasting backlog). Awaiting visual confirm.
- [~] 17. Items tab — `Pages/ItemsPage.swift` ↔ `ItemsPage.kt`
  - Restyled 2026-07-13: five CoinTiles (always all denominations; tap → adjust dialog, kept) · INVENTORY head · ADD ITEM capsule · groups in canonical kind order (weapon→armor→shield→ammunition→potion→scroll→wondrous→tool→gear→treasure) w/ Cinzel kind headers "(n)" · rows Diamond(5) + Cormorant name + "× qty"/damage IM Fell line + seal-check equip toggle + accent@.06 equipped row tint · OTHER PROFICIENCIES (background tools, " · "-joined). Kept: AcquireItem/DropItem/UseItem/AdjustCoin + direct equip toggle; row tap → detail/action dialog standing in for iOS EditItemSheet (#22). Removed Android-only "Equipped" duplicate group (equipped state now shown in-row like iOS). Payload gap: no charge meters on wondrous items. Awaiting visual confirm.
- [~] 18. Lore tab — `Pages/LorePage.swift` ↔ `CodexPages.kt`
  - Restyled 2026-07-13: identity 2-col grid (Background/Alignment, "—" when empty) · MANNER quote blocks (leading accent bar) · BACKSTORY prose · **MINIONS** — CompanionsSection moved here from Combat as expandable r8 cards (name + lifecycle/origin summary + chevron; expanded = statblock + HP stepper + confirmed DISMISS); slice C (2026-07-14) removed the Android-only summon dialog — the section is now view + dismiss only, summoning happens through the Act sheet's summon pickers (matching iOS, where Lore never summons) · FEATURES & TRAITS (race catalogue traits + RaceTraits reminders deduped + chosen feats + background feature). Payload gaps (defer, iOS-only fields): Player/XP, Bearing (age/height/weight/eyes/hair/skin), Allies & Organisations — sections omitted rather than rendered empty. Awaiting visual confirm.
- [~] 19. Actions sheet + pickers — iOS tile grid + ~40 full-screen pickers (`ActionsSheet/`, `ActionPickers/`) · Android: grouped tile sheet + full picker layer (`ui/actions/`) — **structurally complete (slices A+B+C done 2026-07-13/07-14), awaiting visual confirm**
  - Slice A built 2026-07-13: old 6-tile `DnD5eActionsSheet.kt` deleted. New `ui/actions/ActionsSheet.kt` (9-group 2-col tile grid, Cinzel group rules, 26dp Canvas-glyph icon discs, danger tinting, caster/pool/summon/Beast-Master gating from payload) hosted by `CodexShellView` behind the Act pill via `ActionsLayer.kt`; `PickerShell.kt` = shared full-screen-Dialog chrome (CANCEL/kicker/title, ornamental divider, pinned cream-gradient footer, PickerSection/chips/hero-glyph atoms). Eleven pickers in `CorePickers.kt`/`ResourcePickers.kt`: Amount (damage w/ 13 type chips + Other + note; heal; temp HP — damage chains RAW concentration save DC max(10,dmg/2) → `ConcentrationSavePicker` roll → EndConcentration on fail), Note (kind chips, 240 cap), BeginEncounter (inline d20 + INIT chip → AddNote(combat), no encounter intent, matches iOS), ConfirmAction (inspiration gain/spend), Coin (purse row/denoms/gain-spend/source; overspend gated), Condition (apply: 14 standard + Exhaustion + custom; clear: active list — Exhaustion maps to AdjustExhaustion ±1, Android's track model), Rest short/long (This-will bullets + per-effect retain toggles → CancelEffect batch + Short/LongRest; long rest "Prepare spells" row routes to Spells tab), SpendHitDie (pool banner + multiclass die chips + inline roll), ExpendSlot (pact-merged slot chips + reason), SpendResource (pool chips + stepper + note), UseClassFeature (uses/recovery badges).
  - Routed-to-existing tiles (no duplication this slice): Attack roll → `AttackSheet`; Mark death save → `DeathSavesDialog` (made internal); Level up → `LevelUpWizard`; Skill check → Skills tab; Saving throw/Ability check → Stats tab tap-to-roll; Cast a spell → Spells tab cast flow; Manage effects/Concentration → Combat tab sections; Acquire/Use/Drop item → Items tab; summon tiles + Bind companion → Lore Minions.
  - Omitted pending slices B/C (TODOs in `ActionsSheet.kt`): Learn spell (B), dedicated Skill/Ability/Save/Death/Concentration/Attack/Cast-target pickers (B), buff/debuff × ability/save/skill + Ally's spell + Custom effect pickers (C), summon picker ports (C). Recharge at dawn omitted — no `RechargeAtDawn` intent on Android (wondrous charge meters are the #17 payload gap). Deviations: no Heavy Armor Master toggle on Take damage (intent lacks `fromNonmagicalWeapon`); UseClassFeature rows are flat (resolved features carry no class/level/body text); non-2014 top bar lost its dead Actions button (old sheet's 2014 intents always no-op'd on 2024/PF2e — their actions layer is #24/#34 scope). Combat-tab "pending #19" inline sections stay until slice C per spec.
  - **Slice B built 2026-07-13** — the ROLL and COMBAT/CAST pickers; those tiles no longer route to tab surfaces. New `RollPickers.kt` (SkillCheckPicker w/ 18 skill+ability chips, AbilityCheckPicker, SavingThrowPicker w/ "Save against" damage-type/condition chips + Magical-source toggle lighting race-trait auto-advantage — new engine `RaceTraits.advantageOnSave` [Dwarven/Stout Resilience, Fey Ancestry, Brave, Gnome Cunning] flips the d20 to `RollSpec.advantage`, trait line under the dice; DeathSavePicker: tally banner + inline d20 → "Record rolled save" via atomic `RollDeathSave` + manual Success/Failure/Clear tiles via `MarkDeathSave`), `CombatPickers.kt` (AttackPicker — full PickerShell rebuild of the old `AttackSheet.kt` **which is deleted**: target → equipped-weapon chips + "Other…" free-text w/ manual damage → inline d20 (nat 20/1 auto-suggests) → Miss/Hit/Critical chips → crit-doubled weapon damage → riders Sneak Attack + Divine Smite (slot chips, vs-Undead/Fiend, atomic slot expend) + **Savage Attacks** (half-orc melee crit, extra weapon die) → DefeatedToggle → `MakeAttack`; ConcentrationPicker current-banner/switch/end — start/switch commits `ApplyEffect` w/ a concentration-owner custom effect, end = `EndConcentration`; EffectsListPicker triage rows name+impact+duration+END → `CancelEffect`), `CastPickers.kt` (CastSpellChooserPicker over `castableSpellIDs` → shared **CastTargetPicker**: slot chips w/ upcast + ritual toggle [deviation: iOS picks the slot in the library; Android folds it into the picker so both entry paths share one flow], target, apply-to-self for TARGET_PICKED catalogue spells, branch by new `Spell.resolutionKind` [ATTACK d20+outcome / SAVE DC-badges+Failed-Saved chips, save-for-half rolls full damage w/ no per-target prompt / DAMAGE_ONLY / UTILITY], slot-scaled crit-doubled damage w/ manual fallback for unparseable dice, DefeatedToggle → fully-resolved `CastSpell`). The Spells-tab row dialog's Cast now hands off to the same CastTargetPicker (was a fire-and-forget `CastSpell` w/ applyToSelf=true; self-apply is now an explicit toggle, upcast possible). All dice per the spec pattern: selection-keyed inline `RollResultView` (new `onReset` hook tears down stale judgment on reroll/adv-dis flip) → outcome chips (`OutcomeChipRow`/`DefeatedToggle` added to `PickerShell.kt`) → note → commit.
  - Slice B engine extensions (unit-tested, `ActionPickerTests.kt`, tests green): `Spell` decodes SRD `attack_type`/`dc`/`damage` (+`SpellDc`/`SpellDamage`, `resolutionKind`/`dealsDamage`/`saveHalves`/`damageDice(slot, charLevel)`); `CastSpell`/`CastSpellEvent` carry optional resolution (attackD20/total/outcome, saveDC/ability/outcome + new -core `SaveOutcome`, damage/type/halved, targetKilled) folded into the journal summary; `MakeAttack`/`AttackEvent` +`targetKilled` ("— felled", suppressed on a miss); `RollCheck`/`CheckRolledEvent` +`judgedSuccess`+`note` (DC judgment still wins).
  - Slice B routing: `ActionRoute` shrank to LEVEL_UP/SPELLS_TAB/ITEMS_TAB/LORE_TAB — skill/check/save/death/attack/concentrate/manage-effects/cast tiles all mount their own pickers in `ActionsLayer.kt`; `CodexShellView` lost its attack/death-save dialog states; Combat-tab "Make an attack" button + DeathSavesTile now open the new AttackPicker/DeathSavePicker (old `DeathSavesDialog` deleted too). Stats/Skills tap-to-roll `CheckRollDialog` left as-is (page-level cleanup is slice C).
  - Slice B deferrals: **Learn spell tile stays omitted** — Android `InventoryItem` has no scroll→spell link and no `LearnSpellFromScroll` intent (rides the #17 wondrous/scroll payload gap); attack riders Brutal Critical + Improved Divine Smite (no engine helpers yet — iOS-only sections); iOS FeatureReminderChips (`DummyLore.reminders`) not ported (no Android analogue); iOS cast-time effect-option chooser (Enhance Ability variants) deferred — Android's `SpellEffectCatalog` has no option model.
  - **Slice C built 2026-07-14** — effect + summon pickers + the codex page cleanup. New `EffectPickers.kt`: **EffectPicker** (buff/debuff × ability/save/skill — target chips + magnitude [+1/+1d4/+1d6/+2, dice collapse to rounded-down average like iOS] + duration chips + optional source + tinted preview banner → typed `ApplyEffect`; unlike iOS, which folds these into the journal-source string, Android's full `ActiveEffect` model lets the picker build a real `AbilityDelta`/`SaveBonus`/`SkillBonus`), **CustomEffectPicker** (name + source + stacked modifier-row editor + duration → `ApplyEffect(Custom)`; the modifier editor ports at **full depth** — Android's `EffectModifier` hierarchy covers every kind iOS exposes: attack/damage/AC/ability/save[nullable→"all"]/skill/resistance/advantage/note, with the same −5..+5 stepper and free-text kinds; Concentration duration ⇒ concentration-owner, matching iOS), **AllyCastPicker** (free-text ally name + `SpellEffectCatalog.targetable` list → `SpellEffectCatalog.allyCast` `ApplyEffect(ExternalCaster)`). New `SummonPickers.kt`: FindFamiliar/FindSteed (persistent, `SummonCreature` only), BeastCompanion (Bind — HP baked to `max(beastHP, 4×rangerLevel)` at bind time), ConjureAnimals (slot + PHB tier [1×/2×/4×/8× w/ ×2 doubling per 2 slot levels] + species → `CastSpell(concentration)` + `SummonCreature(count)`), AnimateDead (slot + Skeleton/Zombie + PHB scaling `1+2·(slot−3)` → `CastSpell` [non-concentration] + `SummonCreature`), TashasSummonRouter (empty-state — see deviation). All wired in `ActionsSheet.kt` (Effects group grew to 10 tiles) + `ActionsLayer.kt`; summon tiles no longer route to Lore (`ActionRoute.LORE_TAB` removed).
  - Slice C **codex page cleanup** (per the "After slice C" spec paragraph): Combat page is now display-only past the attacks table — the six inline management sections were removed, each with a verified Act path: **Rest**→Short/Long rest tiles, **Inspiration setter**→Spend inspiration tile, **Slot-expend tile**→Expend a slot tile, **Conditions manager**→Apply/Clear condition tiles (exhaustion via ±1), **Class-resources manager**→Spend resource + Use class feature tiles, **Effects manager**→Manage effects tile (+ concentration-save chains off Take damage, ally-cast is the Ally's-spell tile). `ResourcesSection.kt` deleted; `EffectsSection.kt` trimmed to just `EffectDetailDialog` (still used by the Stats effects strip); `ConditionsSection.kt` reduced to a read-only `ConditionsDisplayStrip`. **Conditions display strip placement:** iOS specs no conditions strip on any page, but slice-C guidance calls for a display strip; Android surfaces read-only condition/exhaustion chips on **Stats**, right after the Effects strip (smallest-surprise home; noted divergence). The Lore **Minions** section shrank to view+dismiss only (the old Android summon dialog is gone — summoning is via Act, matching iOS where Lore never summons). Combat keeps its on-page "Make an attack" button (an Android convenience iOS lacks — noted).
  - Slice C deviations/gaps: **AllyCast** implemented (not deferred) but without iOS's party-roster tap-chips (Android has no party roster, #37 — free-text ally instead) and without variant chips for choice spells (Enhance Ability / Enlarge–Reduce — Android's effect templates carry no option shapes; same gap as slice B). **Summon rosters** drive the hand-authored `CreatureCatalog` (≈20 templates + 2 new SRD undead added for Animate Dead) rather than iOS's 330-entry `MonsterCatalog`, so there's no CR-filtered species list or homebrew-creature picker — the tier logic/counts still match PHB (documented deviation; the fuller monster-backed summon path rides the unstarted monster-materialise work). **Tasha's Summon** router shows an empty state only — the `TashasSummonCatalog` + per-spell picker are deferred (Tasha's statblocks aren't openly licensed and aren't in Android's SRD 5.1 content; the tile only appears when a `summon-*` spell is castable, which SRD content never is — mirrors the iOS A18 deferral). **Advance-round** control dropped with the effects manager — iOS has no round tracker; Rounds-bound effects are cancelled via the effect detail card (consistent with A17 round-decay deferral). `RollConcentrationSave` intent is now UI-unreferenced (kept + tested; the Take-damage flow uses `EndConcentration`).
- [~] 20. Level Up — `LevelUp/LevelUpView.swift` ↔ `ui/codex/LevelUpWizard.kt`
  - Rebuilt 2026-07-14: was a single Material `AlertDialog` (one scroll, `FilterChip`s); now a full-screen stepped **`EditorShell`** wizard matching the creation flow — `LuStep` enum CLASS → HP → SUBCLASS(cond) → CHOICES(cond) → REVIEW with `activeSteps` filtering, diamond rail, jump-to-visited (gated by a `furthest` marker), Cancel/Continue/"Level up" footer. Self-hosted full-screen from `CodexShellView`'s existing `levelingUp` gate via a `Dialog(usePlatformDefaultWidth=false)` + parchment `Box` (same pattern as the #19 pickers) — call site + signature unchanged. Steps on the shared atoms: **Class** (existing-class "Name L→L+1" chips + "+ Name" multiclass chips + warn-but-allow prereq banner + selected-class transition label); **HP** (`WizardSegmented` average/roll + `RollResultView` at dieSize 48 in a tile r4, italic "+T HP (die + CON)" preview); **Subclass** (`WizardChipsPicker`, or `WizardTextField` when the class bundles none); **Choices** (`WizardSubSectionCard`s for ASI/feat, fighting style, new cantrips, new spells); **Review** (`ReviewSectionHeader` + `ReviewLine` tile rows). Fires the existing flat `LevelUp` intent unchanged.
  - **HP bug fix**: the old widget passed a `RollBonus("CON", conMod)` into `RollResultView` while `LevelUpMath.levelUpHp` *also* adds `conMod` — a latent double-count / out-of-range (`1..hitDie`) validation risk. Now the widget carries **no** bonus and writes the bare die (`it.keptSum`, 1..hitDie) to `HpChoice.Rolled`; CON is added once, in core, matching iOS. UI-only change — no core/`-2014` touch, so no engine tests needed; `:app:compileDebugKotlin` + `:app:assembleDebug` green.
  - Built vs iOS choice types: ASI/feat (+half-feat +1), Fighting Style, New Cantrips, New Spells — the only ones the flat `LevelUp` intent supports.
  - **Deferred (engine-gap follow-ups, NOT this slice)**: Expertise, Metamagic, Pact Boon, Eldritch Invocation, Spell Swap, and parameterized feats (Resilient / Skill Expert). These need a `LevelChoice` enum + apply branches + catalogues (`InvocationCatalog` / `PactBoonCatalog` / `MetamagicCatalog` / expertise + third-caster advancement) that Android's engine lacks. The Choices step renders only the supported cards.
  - Deviation: Android has no multiclass-prereq engine data, so the Class-step warn-but-allow banner uses a small UI-local PHB ability table (13 in primary ability, standard 5e) rather than an engine `meetsMulticlassPrereqs` helper. Awaiting visual confirm.
- [~] 21. Attack flow — iOS `AttackPicker` ↔ (was `ui/codex/AttackSheet.kt`) now `ui/actions/CombatPickers.kt` AttackPicker
  - Resolved 2026-07-13 as part of #19 slice B: old `AttackSheet.kt` deleted, rebuilt into the PickerShell flow (target → weapon chips / free-text → attack d20 w/ nat 20/1 suggestions → hit/miss/crit → crit-doubled damage → Sneak Attack / Divine Smite (slot drain) / Savage Attacks riders → defeated toggle → `MakeAttack`). Combat-tab "Make an attack" button opens it too. Deferred riders: Brutal Critical, Improved Divine Smite, feature reminder chips. Awaiting visual confirm.
- [~] 22. Add/Edit item sheets — `AddItemSheet.swift`/`EditItemSheet.swift` ↔ dialogs in `ItemsPage.kt`
  - Built 2026-07-14: replaced the Material Add/Edit dialogs with full-screen parchment sheets (new `ui/codex/ItemSheets.kt`) on the `CustomRaceForm` chrome, reusing WizardAtoms + codex tokens. Engine (`:ruleset-dnd5e-2014`, append-only defaulted JSON — no migration): `Inventory.kt` +`ScrollProperties`(spellID/spellName/spellLevel/useScrollDC=false) +`WondrousProperties`(current/max/rechargeDice=1d6+1, `.normalized` clamp) +`InventoryItem` scroll/wondrous/saveBonus/attackBonus/saveBonusByAbility(`Map<Ability,Int>`)/skillBonus(`Map<String,Int>` by skill id)/damageResistances/damageImmunities/conditionImmunities/advantageOn; `ItemKind.displayName` (singular); `WeaponProperties.damageLine`; `DnD5ePayload` +`withItemReplaced`/`withItemRemoved` extension helpers. Codec round-trip test extended (new `magic-item rider fields round-trip`); **337 `:ruleset-dnd5e-2014` tests green**. **AddItemSheet**: 4-state search flow (empty hint · MATCHES(n) diamond rows · catalogue preview card + quantity stepper · custom homebrew card) with kind-gated sub-forms (weapon Melee/Ranged segmented + damage dice + 13-canonical damage-type chips + "Other…" free-text + comma properties + ranged ranges + attack bonus; armor Light/Medium/Heavy segmented + baseAC/DEX-cap/STR/stealth + AC & save bonus; scroll spell chip/CTA; wondrous charge fields; AC/save bonus per kind) → `AcquireItem`. **EditItemSheet**: name/kind(read-only)/quantity/equipped/AC-save-attack bonus steppers, read-only weapon/armor summary cards, scroll section, wondrous Track-charges toggle (defaults 7/7 1d6+1) + steppers + recharge dice, notes, collapsible **Advanced Effects** (per-ability save grid, skill-bonus rows + add-menu, 4 comma-list resistance/immunity/advantage fields); Delete + Save → direct `withItemReplaced`/`withItemRemoved` payload edit (unjournaled, iOS parity); **Use/Drop kept reachable** via a small action row. **ScrollSpellPicker**: search over the full spell library, grouped by level. `:app:compileDebugKotlin` + `:app:assembleDebug` green. Awaiting visual confirm.
  - Deferred: **runtime mechanics out of scope** — wondrous-charge consume + scroll-DC cast are data-storage only (`UseItem` unchanged). **Advanced-effects folding into AC/saves/skills is A17 territory** — the editor stores the fields; whether they fold follows the active-effects system. **ScrollSpellPicker source-filter divergence**: gated to the full `DnD5eCatalog.spellLibrary`, NOT `enabledSources` — no `spells(in: sources)` accessor exists on the Android catalogue (payload `enabledSources` is data-only), so a PHB-only character can currently link a supplement spell to a scroll. Minor: `skillBonus` keyed by skill id string (`Map<String,Int>`) rather than iOS's `[Skill:Int]` — Android's `Skill` is a non-serializable data class.
- [~] 23. Identity sheet — iOS `Identity/IdentitySheet.swift` ↔ `ui/identity/IdentitySheet.kt`
  - Built 2026-07-14 (Android had none). **Portrait persistence (the real work)** — Room **v5→v6**: `PersistentCharacter.portraitData` nullable BLOB + `MIGRATION_5_6` (`ALTER TABLE characters ADD COLUMN portraitData BLOB`, registered in `Nat20Data`, exported `6.json`, SQL matches schema); `CharacterCodec` maps it entity↔domain and via the JSON envelope as Base64 (campaign snapshots carry it); 2 new `:data` codec tests (portrait bytes round-trip through row + Base64 envelope; null stays null) → **11 `:data` tests green**. **New `PortraitPickerControl`** (`ui/identity`): 58×70 double-stroked parchment tile (person placeholder gradient when empty / cropped image when set), "Add a portrait"/"Portrait chosen" + helper, danger "Remove" when set; owns a `PickVisualMedia(ImageOnly)` launcher + a bounded-sampler downscale helper (Uri→ContentResolver/BitmapFactory `inSampleSize`→512px max→JPEG 80→ByteArray). **New `IdentitySheet`** on the shared `ActionPickerShell` chrome (kicker "Identity" / title "Edit identity" / CANCEL back / footer `WizardPrimaryButton("Save", enabled = trimmed name non-empty && (name or portrait changed))`; body = picker + `WizardFieldLabel("Name", required)` + `WizardTextField`). **Wired**: `CodexShellView` HeroRow is clickable **only in-campaign** → opens the sheet seeded from the character → on commit `onSave(character.copy(name, portraitData, updatedAt = now))` (cosmetic, unjournaled — not `onApplyIntent`); HeroRow + roster card render the portrait via shared `CharacterPortraitFrame`, falling back to `DropCapSquare`. `:app:assembleDebug` green. Awaiting visual confirm.
  - Deviations: `ActionPickerShell` stayed `internal` (same `:app` module → usable from `ui.identity`, no widening needed). Domain `Character` keeps its `ByteArray` reference-equality (the class already carries that note) — not needed for recomposition correctness (reference inequality only ever over-recomposes, never skips a needed one), so the portrait test compares bytes with `assertArrayEquals` rather than `assertEquals`. No live Room migration test (`:data` JVM suite has no Robolectric harness; v5→v6 SQL matches `6.json` verbatim). Creation wizards don't yet adopt the picker (in-campaign identity sheet only, per spec scope).

## Character sheet — 5e 2024 codex

- [~] 24. Codex shell/chrome — `Codex2024ShellView.swift` ↔ `ui/sheet/Codex2024ShellView.kt`
  - Built 2026-07-14 (slice 1 of 3): **chrome extraction (pure refactor) + 2024 restyle onto it.** Pulled the 2014 shell's nav/hero/campaign/tab-bar chrome into a new payload-agnostic `ui/codex/CodexChrome.kt` → `CodexScaffold(pagerState, tabLabels, heroName, heroSubtitleLines, portraitData, fallbackLetter, editableHero/onEditIdentity, inCampaign/campaignName/hasPastAdventures, onBack/onEdit/onAct?/onStartCampaign/onEndCampaign/onOpenJournal/onOpenPastAdventures, pageContent(page))`; moved TopNavRow (+Act/Edit pills), HeroRow (portrait/drop-cap + name + N subtitle lines), CampaignRegion (Start Adventuring / Past Adventures / journal bar + End), the diamond tab bar and BookGlyph into it. Caller owns the `PagerState` so it can still animate to a tab (2014 actions layer). Refactored `CodexShellView` (2014) to call `CodexScaffold` with **zero behaviour change** — verified by `:app:compileDebugKotlin` green with the 2014 shell untouched structurally (same component order, same params, hero lines/campaign/tab logic identical) before any 2024 work began. TopNavRow pill logic: in-campaign+`onAct!=null` → Act pill; building → Edit pill; else (2024 in-campaign, no actions layer yet) → no pill.
  - Dispatch: `CharacterSheetScreen` now routes 2024 to a bare chrome-owning `Codex2024ShellView` (new signature mirroring 2014: campaign/back/edit/journal/past callbacks + onApplyIntent/onSave), exactly like the 2014 branch. PF2e stays on the old Scaffold+TopAppBar else-branch (that's #34). Start/End campaign dialogs stay hosted in the host. Act pill wired to `null` this slice (2024 actions layer = #31/slice 2).
- [~] 25–30. Six tabs (Stats/Skills/Combat/Spells/Items/Lore) — `Codex2024Pages.swift` ↔ `Codex2024ShellView.kt`
  - Built 2026-07-14 (slice 1 of 3): rebuilt the 820-line stock-Material monolith onto `CodexScaffold` + the parchment `CodexAtoms` (SectionHead/StatChip/HPBlock/AbilityMedallion/SaveCell/SkillPip/SlotTile/CoinTile/CodexButton/CodexCapsuleButton/InspirationBanner/CodexEffectChip/FeatureBlock/IdentityCell/CodexPage), deleting the in-file Material atoms (Card2024/StatRow/Medallion2024/Page2024/etc). Hero subtitle = species (`DnD5e2024Catalog.species(...).name` fallback slug) + "Level N {Class}". Per-tab deltas applied:
    - **Stats** (#25): chip row Init/AC(tap→breakdown)/Prof/Speed · HP compact · ability medallion grid (tap-to-roll kept) · saves strip (tap-to-roll) · effects strip (tap→CancelEffect2024) · Conditions section with the **2024 cumulative exhaustion line** ("Exhaustion N" → "{signed} to d20 Tests · −{ft} Speed" via `Exhaustion2024.d20Modifier/speedPenaltyFeet`, NOT the 2014 per-level table) + active-condition chips · Defenses (folded damage resistances) · **heroic InspirationBanner** (`payload.hasInspiration`) · Level Up button.
    - **Skills** (#26): identical layout to 2014 (passive-perception callout + 18 dashed rows + tap-to-roll) but **binary proficiency only** — SkillPip forced `expertise=false`, legend drops the expertise tier.
    - **Combat** (#27): HP large · Armor(tap)/Init(tap-roll/clear)/Speed chips · HitDice + DeathSaves tiles · **Attacks table** (known weapons from `weaponMasteries`, dagger fallback; rows show ability·**mastery.displayName** subtitle + atk via `AttackMath2024` + damage) · **WEAPON MASTERY** SectionHead + rows (`WeaponMastery2024.from → displayName/summary`) · Fighting Style · Active Effects (concentration banner + chips). Android's interactive vitals stay on-tab (no actions layer yet, #31): Make-an-attack→`AttackSheet2024`, Vitals damage/heal/temp, exhaustion ±, inspiration grant/spend, conditions add/clear, long rest — all restyled onto SectionHead + CodexButton.
    - **Spells** (#28): "all casters prepare" — CANTRIPS · n and PREPARED · n sections (no "Spells Known"), empty prepared = "No spells prepared.", concentration line, slot tiles, browse → SpellLibrary. **RECONCILED the onSave(copy) hack** → prepared add/remove now fire `PrepareSpell2024`/`UnprepareSpell2024` intents (journaled); cantrips-known stays a direct list edit (no cantrip intent exists — cantrips aren't "prepared" in 2024).
    - **Items** (#29): coin tiles (tap→adjust) · **kind.displayName rows** (no mastery/armor annotations) with drop via `DropItem2024` · the 2024 armor/shield/mastery gear editors **kept & restyled** (AC breakdown line, Change armor / Stow-Equip shield / Edit masteries via existing intents + dialogs).
    - **Lore** (#30): Species+Background identity grid · **ORIGIN FEAT** (`payload.originFeat`) · **SPECIES TRAITS** (`SpeciesTraits2024.reminders`, fallback to catalogue trait names) · Feats & Style (chosenFeats + fightingStyle) · **ALIGNMENT**. No manner/personality/ideal/bond/flaw, no minions (2024 has none).
  - Deviations / gaps (Android 2024 payload/stats are slimmer than the iOS `CharacterStats2024` read-model): **no cantrips/prepared limit** helper → sections show "· n" not "n/limit" (iOS `cantripsKnownLimit`/`preparedSpellLimit`); **no resource-pool / languages / advantage-defense** helpers → those Stats sub-sections omitted; **no `backstory` field** on the payload → Lore Backstory block omitted; **no class-feature catalogue** → Combat/Lore "Class Features" omitted. Effect chips cancel on tap (no detail dialog). Speed read from the species catalogue (base, no exhaustion subtraction). The kept gear/spell/condition/amount editors remain functional Material `AlertDialog`s (deep parchment restyle deferred with the actions layer). AttackSheet2024 + LevelUp2024Wizard mounted as-is (restyle = slice 3, #32/#33).
- [~] 31. 2024 actions sheet + pickers — `Actions2024SheetView.swift` + `Pickers2024.swift` ↔ `ui/actions/Actions2024Sheet.kt` + `Actions2024Layer.kt` + `Actions2024Pickers.kt`
  - Built: parallel 2024 actions layer (does NOT reuse the 2014 `ActionsSheetView`/`DnD5eActionsLayer` — those are `DnD5ePayload`-typed; mirrored the tile/group/glyph visuals into `Actions2024Sheet.kt` since the 2014 render composables are `private`). Reused the payload-agnostic `PickerShell` chrome + shared pickers (`AmountPicker`, `ConditionPicker`, `ConcentrationSavePicker`).
  - **5 groups** (iOS parity — simpler than 2014's 9): **Combat** [Attack→`AttackSheet2024` (gated `weapons.isNotEmpty`), Cast→`Cast2024Picker`→`CastSpell2024` (gated caster, slot-gated leveled rows)] · **Vitals** [Take Damage→`TakeDamage2024` + chained `ConcentrationSavePicker` when concentrating, DC max(10,hpDmg/2)→`EndConcentration2024`; Heal→`Heal2024`; Gain Temp HP→`GainTempHp2024`] · **Inventory** [Add Item→`ItemEntry2024Picker`→`AcquireItem2024`; Add Gold→`AdjustCoin2024(GP,+n)`; Spend Gold→`AdjustCoin2024(GP,−n)`] · **Progress** [Level Up (L<20)→`LevelUp2024Wizard`; Long Rest→`LongRest2024`; Heroic Inspiration→`SetInspiration2024`] · **Conditions** [Exhaustion ±1→`ChangeExhaustion2024`; Apply/Clear Condition→`ApplyCondition2024`/`ClearCondition2024`, Clear also lists the exhaustion track row]. Act pill wired in `Codex2024ShellView` (`showActions && inCampaign`, gated like 2014).
  - Deviations: **"Spend Resource" + "Short Rest" tiles omitted** — the Android `:ruleset-dnd5e-2024` module never ported `SpendResource2024`/`ShortRest2024` intents nor the `availableResourcePools()` accessor (iOS has all three); nothing to fire, so tiles dropped (domain-slice gap, not UI). Damage entry stays plain (no damage-type/resistance step) matching iOS 2024's simpler `AmountEntrySheet2024`. Amount pickers reuse Android's number-pad `AmountPicker` (established parchment idiom) rather than iOS's 1..999 stepper. Concentration lucky-reroll passed `false` (2024 species luck not modelled on the roll primitive).
  - Overlap reconciliation: the Combat tab's inline vitals/exhaustion/inspiration/conditions/long-rest editors + Material `AlertDialog`s (`AmountDialog`/`ConditionPickerDialog`) are **KEPT** as the building-phase direct-edit surface (the Act pill only shows in-campaign, so removing them would leave building characters unable to edit HP/conditions/rest). Non-duplicative in practice: layer = in-campaign Act path, tab = building path.
- [~] 32. 2024 Level Up — `LevelUpSheet2024.swift` ↔ `ui/sheet/LevelUp2024Wizard.kt`
  - **Built (slice 3):** rebuilt from a stock Material `AlertDialog` into a full-screen **stepped `EditorShell` wizard** on the parchment chrome, mirroring the 2014 `LevelUpWizard`. Steps: **Class** (advance-existing chips + multiclass chips) → **Hit Points** (`WizardSegmented` Average/Roll + inline `RollResultView`) → **Subclass** (conditional at the class's `subclassLevel`, `WizardChipsPicker` or free-text) → **Choices** (conditional on `needsAsi`; `WizardSubSectionCard` with ASI +2/+1 `WizardMultiSelectChips` vs General-feat `WizardChipsPicker` + half-feat +1 pick) → **Review** (`ReviewSectionHeader`/`ReviewLine`). Fires the existing flat `LevelUp2024` intent unchanged; deleted the in-file `Label2024` atom. Self-hosted full-screen from `Codex2024ShellView` (Stats-tab Level Up) + `Actions2024Layer` (Progress tile) — both call sites unchanged (same signature).
  - **Android richer than iOS 2024:** iOS 2024's `LevelUpSheet2024` is a minimal single sheet (subclass-at-L3 + ASI/feat, auto-skipped when no choice). Android keeps its richer 2014-parity flow — the **multiclass picker** and the **average/rolled HP** choice — which iOS 2024 omits.
  - **Simpler than 2014 (by design):** the `LevelUp2024` intent models **no** fighting-style / new-cantrip / new-spell picks at level-up, so (unlike the 2014 wizard) the Choices step carries only the ASI/feat row. No multiclass-prereq warning banner (2024 prereqs differ; not modelled).
  - **HP-bug status:** the 2024 path was **already numerically correct** — the roll widget reads `RollResult.keptSum` (bare kept-dice sum, excludes bonus chips), so `HpChoice.Rolled` always received the die alone (in `1..hitDie`, passing `LevelUp2024`'s validation); `LevelUpMath.levelUpHp` adds CON. No double-count existed. Still dropped the CON bonus chip from the widget (bare die display) to match the 2014 fix and keep the readout honest.
- [~] 33. 2024 attack flow — `AttackPicker2024` ↔ `ui/sheet/AttackSheet2024.kt`
  - **Built (slice 3):** rebuilt from a stock Material `AlertDialog` onto the shared `ActionPickerShell` parchment chrome, mirroring the 2014 `AttackPicker`. Flow: **Target** (`WizardTextField`) → **Weapon** (`PickerChip` row, one per pack weapon) → **mastery reminder** (`PickerHelpText` = `mastery.displayName — summary`) → **Attack roll** (inline `RollResultView` d20 + `attackBonuses`; natural 20/1 auto-suggests Critical/Miss) → **Outcome** (`OutcomeChipRow` Miss/Hit/Critical) → **Damage** (crit-doubles the dice via `damageSpec.critDoubled()`) → `PrimaryActionButton` fires the existing `MakeAttack2024`. Deleted the in-file `SectionLabel2024`/`OutcomeChip2024` atoms. Self-hosted from `Codex2024ShellView` Combat-tab Attack button + `Actions2024Layer` Attack tile — both call sites unchanged.
  - **2024-specific:** surfaces the chosen weapon's **weapon mastery** (the 2024 combat lever). Deliberately **no 2014 damage riders** (Sneak Attack / Divine Smite / Savage Attacks) — 2024 replaces them with masteries.
  - **Android richer than iOS 2024:** iOS 2024's attack is a flat weapon list resolved centrally in the engine; Android keeps its **staged roll flow** (weapon → attack roll → outcome → damage), matching its 2014 picker. Divergence noted; behavior preserved.

## Character sheet — PF2e

- [ ] 34. PF2e sheet — iOS single-scroll read-only `CodexPathfinderShellView.swift` · Android 6-tab pager `ui/sheet/PathfinderSheetView.kt` — **structural rebuild**
- [ ] 35. PF2e actions sheet — `PathfinderActionsView.swift` · Android: none — **missing**
- [ ] 36. PF2e Level Up — `PathfinderLevelUpView.swift` ↔ `PfLevelUpDialog` (in `PathfinderSheetView.kt`)

## Campaign

- [ ] 37. Campaign setup/settings — iOS `CampaignSetupView.swift` + `CampaignSettingsView.swift` (party roster, leave campaign, 2014 daily-spells) · Android: start/end dialogs only — **structural**
- [ ] 38. Journal — `CampaignJournalView.swift` (day-session pager, Log/Chronicle toggle, Act button, cog) ↔ `ui/journal/JournalScreen.kt`
- [ ] 39. Past adventures — `PastAdventuresView.swift` ↔ `ui/past/PastAdventuresScreen.kt`
- [ ] 40. Character settings (PDF export, delete) — iOS `CharacterSettingsView.swift` · Android: **missing** (delete = roster swipe)

## Reference (Settings → …)

- [ ] 41. Spell library shell + 3 ruleset tabs — `ReferenceTabShell.swift` + per-ruleset views ↔ `ui/reference/SpellLibraryShell.kt`
- [ ] 42. Item catalog + details — per-ruleset ↔ `ItemCatalogShell.kt` (iOS pushes detail screens; Android uses dialogs)
- [ ] 43. Monster codex + statblocks — per-ruleset ↔ `MonsterCodexShell.kt`
- [ ] 44. Custom Creatures list + form — iOS only · Android: **missing**
- [x] 45. PF2e Actions reference — removed 2026-07-13 (Android-only, no iOS counterpart; see #4)

## Out of scope for this audit

- AI character creation (backlog A20, unstarted on Android by design)
- PDF export pages (follows from #40 if built)
- iPad/tablet layouts (A13 descoped — Android is phone-only)
