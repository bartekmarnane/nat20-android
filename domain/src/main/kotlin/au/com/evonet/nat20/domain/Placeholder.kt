package au.com.evonet.nat20.domain

/**
 * Ruleset-agnostic core lives here. Step A2 fills this in with the
 * Ruleset / Character / CharacterPayload / CharacterEvent / CharacterIntent
 * abstractions ported from the iOS `Domain` Swift package.
 *
 * Placeholder so the module compiles before A2 lands.
 */
internal const val DOMAIN_MODULE = "nat20-domain"
