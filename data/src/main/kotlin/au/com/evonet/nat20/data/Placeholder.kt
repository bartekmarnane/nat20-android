package au.com.evonet.nat20.data

/**
 * Persistence + repository layer. Step A5 adds Room entities/DAOs and a
 * CharacterRepository interface (local-only to start; the interface is the
 * seam a sync backend slots into later — see README "Open decisions").
 *
 * Placeholder so the module compiles before A5 lands.
 */
internal const val DATA_MODULE = "nat20-data"
